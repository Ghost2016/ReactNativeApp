## 组件说明
1. 时间轴组件
```javascript
props = {
    startTime: string,
    endTime: string,
    subInfo: string | Function | React.Element,
    footer: ?(string | Function | React.Element),
    dotColor: ?string
}
```