/* @flow */
// import type { Auth, Topic } from '../types';
import { apiFetch, apiFetchOther } from "./apiFetch";

/* export default async (params: object, callBack: () => {}) => {
  console.log("zxy", params)
  // return apiFetch(`/v1/other/calc_salary/`, callBack, params, "POST");
  return apiFetchOther(
    `https://sxz.zhimekaimen.com/salary_predict_pro?p=${JSON.stringify(params)}`,
    callBack,
    {},
    "GET"
  );
}; */

